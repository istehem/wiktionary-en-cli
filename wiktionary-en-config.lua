require("config")
require("formatter")

format = formatter.format
intercept = nil

-- Enable a configuration of standard values
-------------------------------------------------------------------------------
config.language = "en"
