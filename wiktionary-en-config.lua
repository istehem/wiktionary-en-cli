require("config")

format = config.format
intercept = nil

-- Enable a configuration of standard values
-------------------------------------------------------------------------------
config.language = "en"
